# 项目结构说明

逐个文件讲清楚"这是干什么的"，翻代码时对照着看。

- 想知道**怎么跑起来、怎么部署** → 看 [../README.md](../README.md)
- 想知道**某个文件是干嘛的、该改哪里** → 看本文

---

## 一、先理解分层：谁依赖谁

```
前端 Vue ──HTTP──> routers/    接口层：URL、权限、状态码
                      ↓ 调用
                   services/   业务层：抽题、判分、解析、导出
                      ↓ 读写
                   models.py   数据层：表结构
                      ↓
                   database.py → SQLite / MySQL
```

改东西时的定位方法：

| 要改什么 | 去哪 |
|---|---|
| 接口地址、参数、权限 | `app/routers/` |
| 算法、业务规则 | `app/services/` |
| 数据库字段 | `app/models.py` + 新建 alembic 迁移 |
| 学校名、默认题量、及格线、导入规则 | `config.yaml`（不用改代码） |
| 页面长相、交互 | `frontend/src/views/` |

三层不要互相越级：路由里不写算法，服务里不碰 HTTP。

---

## 二、顶层文件

| 文件 | 作用 |
|---|---|
| `README.md` | 项目总说明书。怎么跑、表结构、接口清单、维护方法、已知待办 |
| `一键启动.bat` | **给老师用的**。检查环境 → 装依赖 → 建库 → 导题 → 起前后端 → 开浏览器，六步全自动。第一次约 3~5 分钟，之后十几秒 |
| `一键停止.bat` | 停服务。只杀占用 8000/5173 的 python/node 进程，别的程序占同端口会跳过，不会误杀 |
| `config.yaml` | **集中配置**：学校名、默认题量、及格线、导入规则、上传限制。改完重启后端生效，前后端共用这一份 |
| `docker-compose.yml` | **服务器部署用**。定义 MySQL / 后端 / 前端三个容器、数据卷、健康检查 |
| `.env.example` | 部署配置模板（数据库密码、JWT 密钥、管理员密码、端口）。复制成 `.env` 再改 |
| `.gitignore` | 排除 `.venv`、`node_modules`、`data/`、`.env` 等不进版本库的东西 |

---

## 三、backend/ —— Python 后端

### 3.1 骨架层

每个文件只管一件事，出问题时能快速定位。

| 文件 | 行数 | 作用 |
|---|---|---|
| `app/main.py` | 40 | **入口**。创建 FastAPI 应用、挂 CORS、挂 `/uploads` 静态目录、注册 8 个路由 |
| `app/config.py` | 41 | **密钥与环境**相关配置，从环境变量读。改端口/密钥只动 `.env` |
| `app/siteconfig.py` | 147 | **读 `config.yaml`** 并严格校验。键名拼错、类型不对一律启动即报错并指出是哪一项，不静默失效 |
| `app/database.py` | 30 | 数据库连接与会话。SQLite 和 MySQL 都走这里，切换只改 `DATABASE_URL` |
| `app/models.py` | 180 | **9 张表的定义**，见 3.4 |
| `app/schemas.py` | 289 | 接口的出入参格式。`TakeQuestionOut` 故意不含 `answer` 字段 —— 答案不泄漏的第一道防线 |
| `app/security.py` | 32 | bcrypt 密码哈希 + JWT 令牌签发/校验 |
| `app/deps.py` | 34 | 登录校验 `get_current_user`、管理员校验 `require_admin`，接口上挂一行就生效 |
| `app/constants.py` | 17 | 字典分类名等真常量；知识范围、题型等只是从 `config.yaml` 转出来的别名 |
| `app/seed.py` | 64 | 首次启动建管理员账号、灌字典表。可重复执行不出错 |

> ⚠️ 知识范围和题型有三个层次：`config.yaml` 是**建库种子** → `dict_items` 表是**运行期真相** →
> `constants.py` 只是把种子值转出来的别名。想加一类知识范围，改数据库（走界面），不要改代码。

### 3.2 services/ —— 五个核心算法（最值得看的部分）

这几个文件的规则参数（题型关键词、答案写法、分隔符、卷号前缀等）都读 `config.yaml`，不写死。

| 文件 | 行数 | 内容 |
|---|---|---|
| `app/services/sampler.py` | 69 | **均衡抽题**。名额在十个知识范围之间轮流分配，某范围题不够时名额自动让给别人。从原 HTML 的 `allocate`/`pick` 原样移植 |
| `app/services/paper.py` | 137 | **组卷排版**。调 sampler 抽题 → 打乱选择题选项（答案自动跟随）→ 按题型分大题 → 生成卷号 |
| `app/services/importer.py` | 244 | **Excel/CSV 解析与逐行校验**。题型识别、判断题答案归一（√/×/对/错/T/F）、选项拆分、题干哈希查重。从原 `handleRows` 移植 |
| `app/services/exam.py` | 164 | **考试取卷与判分**。`strip_answers()` 去答案发给学生，`grade()` 在服务端判分 |
| `app/services/export.py` | 225 | 三种导出：题库 Excel、本卷 Excel、学生答题网页（图片转 data URI 内嵌，可独立分发） |

> 🔒 **答案只在 `exam.py` 的 `grade()` 里被读到。** 改这个文件要格外小心，改完必跑测试。

### 3.3 routers/ —— 八个接口文件

| 文件 | 行数 | 管什么 |
|---|---|---|
| `app/routers/auth.py` | 82 | 登录、改密码、教师账号增删（管理员） |
| `app/routers/dicts.py` | 69 | 知识范围/题型清单。**加新知识范围只调这个接口** |
| `app/routers/questions.py` | 245 | 题库增删改查、分页筛选、统计、配图上传、题库导出 |
| `app/routers/imports.py` | 136 | 下载模板、上传解析入库、导入记录 |
| `app/routers/papers.py` | 215 | 组卷、名额预览、存档、历史试卷、两种导出 |
| `app/routers/exams.py` | 232 | 发布考试、四个开关、成绩列表、答卷明细、题目分析、成绩导出 |
| `app/routers/take.py` | 89 | **学生端，公开访问**。整个文件不引用任何鉴权依赖，也不返回答案 |
| `app/routers/config.py` | 35 | 把 `config.yaml` 里跟界面有关的部分发给前端。公开，只发安全字段 |

### 3.4 数据库表（`models.py` 里的 9 张）

| 表 | 存什么 |
|---|---|
| `users` | 账号、bcrypt 哈希、姓名、角色（admin/teacher）、是否停用 |
| `dict_items` | 知识范围、题型两类枚举 —— 加一类只改数据不改代码 |
| `questions` | 题型、题干、答案、知识范围、来源、图片路径、必出标记、**软删除标记**、题干哈希 |
| `options` | A/B/C/D 选项，随题目级联删除 |
| `papers` | 存档试卷的抬头：标题、学校、时长、卷号 |
| `paper_items` | 试卷的题目顺序 + **打乱后的选项快照**（重开历史卷和当初印的一模一样） |
| `exams` | 发布的考试：口令、开放状态、四个开关 |
| `exam_submissions` | 学生答卷：原始作答、判分明细、得分 |
| `import_logs` | 谁、何时、导了什么文件、成功失败几条、原件存哪 |

两个刻意的设计：

- **题目软删除**（`is_deleted` 标记）而非物理删除，误删能恢复
- **图片存文件不存 base64**，数据库里只记路径，不会被撑爆

### 3.5 alembic/ —— 表结构版本管理

| 文件 | 作用 |
|---|---|
| `alembic/env.py` | 迁移运行环境，从 config 读数据库地址 |
| `alembic/versions/0001_init.py` | 初始 7 张表 |
| `alembic/versions/0002_paper_header.py` | 试卷抬头（学校/时长/卷号）+ 选项快照 |
| `alembic/versions/0003_exams.py` | 考试与答卷两张表 |

**以后改表结构一律新增迁移文件，不手工改表：**

```bash
cd backend
alembic revision --autogenerate -m "给题目加难度字段"
alembic upgrade head
```

容器启动会自动执行 `alembic upgrade head`，服务器上不用手动跑。

### 3.6 tests/ —— 74 个用例

| 文件 | 覆盖 |
|---|---|
| `tests/conftest.py` | 测试隔离：临时目录 + 临时 SQLite，**不碰 `data/app.db` 里的真实题库** |
| `tests/test_logic.py` | 抽题算法、打乱选项、Excel/CSV 校验的边界 |
| `tests/test_api.py` | 真实链路：登录、建题查重、下载模板再传回去导入、组卷存档、三个导出 |
| `tests/test_exam.py` | 考试全流程：发布 → 取卷 → 交卷 → 判分 → 成绩汇总 |
| `tests/test_config.py` | 坏配置必须报错、好配置必须真的改变行为。保护「改 YAML 就生效」这条链路 |

跑测试：

```bash
cd backend && .venv\Scripts\python.exe -m pytest -q
```

> 🔒 `test_exam.py` 里的 `test_answers_never_reach_the_student` 会逐字检查学生响应中不含任何答案。
> **改考试相关代码后一定要确认这条还过。**

### 3.7 tools/ 与配置文件

| 文件 | 作用 |
|---|---|
| `tools/migrate_from_html.py` | 把老 HTML 里的 341 题 + 26 张 base64 图片搬进数据库，按题干哈希去重，重复跑不会重复灌 |
| `requirements.txt` | 运行依赖 |
| `requirements-dev.txt` | 测试依赖（pytest、httpx） |
| `entrypoint.sh` | Docker 容器启动脚本：等数据库就绪 → 迁移 → 灌种子 → 起服务 |
| `Dockerfile` | 后端镜像构建 |
| `pytest.ini` | 测试配置（`pythonpath`、测试目录） |
| `.env` | **实际生效的配置，不进 git**。首次启动时从 `.env.example` 复制而来 |

---

## 四、frontend/ —— Vue 3 前端

### 4.1 骨架

| 文件 | 作用 |
|---|---|
| `src/main.js` | 挂载 Vue、Element Plus、图标、中文语言包 |
| `src/App.vue` | 顶部导航 + 深色模式开关。学生页和登录页走"裸布局"，不套教师界面 |
| `src/router.js` | 路由与登录守卫。`/take/:token` 标了 `public`，学生不用登录 |
| `src/api.js` | 所有接口调用集中在这里。自动带 JWT、401 自动跳登录、统一错误提示 |
| `src/siteConfig.js` | 前端这侧的 `config.yaml` 副本，启动时从 `/api/config` 取回，各页面直接读 |
| `vite.config.js` | 开发服务器与代理。端口冲突时用 `VITE_API_TARGET` 换后端地址 |
| `index.html` | 页面骨架，只有一个挂载点 |

### 4.2 views/ —— 七个页面

| 页面 | 行数 | 谁用 | 内容 |
|---|---|---|---|
| `views/Login.vue` | 87 | 教师 | 登录 |
| `views/Paper.vue` | 657 | 教师 | **组卷**（最复杂）。抬头设置、题量、三模式切换（试卷/答案卷/在线自测）、换一批、打印、两种导出 |
| `views/Questions.vue` | 323 | 教师 | 题库管理。筛选、增改删、必出标记、配图、导出 |
| `views/Import.vue` | 180 | 教师 | 模板下载与上传，导入结果逐行反馈 |
| `views/Exams.vue` | 370 | 教师 | 考试管理。发布、复制链接、四个开关、成绩汇总、错题分析、导出 |
| `views/Take.vue` | 332 | **学生** | 答题页，**免登录**。拿不到答案，交卷后分数由后端返回 |
| `views/Users.vue` | 125 | 管理员 | 教师账号管理 |

### 4.3 部署相关

| 文件 | 作用 |
|---|---|
| `nginx.conf` | 生产环境：托管静态文件、代理 `/api` 和 `/docs`、SPA 路由回退 |
| `Dockerfile` | 两阶段构建：node 编译 → nginx 托管 |
| `package.json` / `package-lock.json` | 依赖清单与锁定版本 |

---

## 五、其余目录

| 目录 | 内容 |
|---|---|
| `legacy/` | 老版本单文件 HTML（2.5 MB）。既是存档，也是 `migrate_from_html.py` 的默认数据源 |
| `scripts/` | `run-backend.bat` / `run-frontend.bat`，被一键启动调用，也可单独双击 |
| `backup/` | `backup.sh`，加进 crontab 每天备份数据库和上传目录，保留 30 天 |
| `docs/` | 本文所在目录 |

### 运行时自动生成（不进版本库）

| 路径 | 内容 |
|---|---|
| `backend/data/app.db` | **真实题库，331 道题都在这里** —— 最该备份的文件 |
| `backend/data/uploads/images/` | 题目配图 |
| `backend/data/uploads/imports/` | 教师上传的 Excel 原件留底 |
| `backend/.venv/` | Python 虚拟环境，删了重装即可 |
| `frontend/node_modules/` | 前端依赖，删了 `npm install` 即可 |
| `frontend/dist/` | 前端构建产物 |

---

## 六、三条维护要点

1. **加新知识范围** → 调 `POST /api/dicts`（或直接改 `dict_items` 表），前端下拉框和导入校验立刻生效，不用改代码、不用重新部署
2. **改表结构** → 写 alembic 迁移，不手工改表
3. **改考试相关代码** → 跑 `pytest`，特别确认 `test_answers_never_reach_the_student` 还过

---

## 七、两条容易踩的坑

- **`config.yaml` 里的 `bank.scopes` 不是运行期的知识范围清单**。它只在第一次建库时灌种子，之后系统以数据库 `dict_items` 表为准，日常增删走界面。想让文件说了算，把 `bank.sync_on_start` 改成 `true`。
- **新加可配置项要走三步**：在 `config.yaml` 加字段 → 在 `siteconfig.py` 的对应模型里声明（否则 `extra="forbid"` 会报错）→ 用到的地方读 `site.xxx`。前端要用的，还得在 `routers/config.py` 里放行。
- **学生端接口不要图省事复用教师端的 schema**。`TakeQuestionOut` 之所以单独定义且不含 `answer`，就是为了让"发答案给学生"这件事在类型层面做不到。复用 `QuestionOut` 会直接泄漏答案。

---

*本文对应的代码状态：2026-09-03。文件数、行数会随开发变化，改动较大时记得同步更新本文。*
